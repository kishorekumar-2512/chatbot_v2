import AppShell from './components/layout/AppShell.jsx';

/**
 * App — Root component.
 * Delegates to AppShell which handles org login, layout, and all sub-components.
 */
export default function App() {
  return <AppShell />;
}
