# Core framework
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.10.1
python-dotenv==1.0.1

# Database
psycopg2-binary==2.9.9

# HTTP client (LLM API calls, Vision Agent)
httpx>=0.27.0
requests==2.32.2

# Data processing & visualization
pandas==2.2.2
plotly==5.22.0
kaleido==0.2.1
fpdf2==2.7.9

# Semantic retrieval — Hybrid RAG (dense + sparse)
sentence-transformers>=2.7.0
chromadb>=0.5.0
rank-bm25==0.2.2

# Schema graph — GraphRAG (FK relationship engine)
networkx==3.3

# Rate limiting
slowapi==0.1.9

# MCP server architecture
mcp==1.2.0

# JWT authentication (production SSO)
PyJWT>=2.8.0
cryptography>=42.0.0

# Markdown support (documentation export)
markdown>=3.6

# Legacy Streamlit UI (optional — only if using frontend/app.py)
streamlit==1.35.0
