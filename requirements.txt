# Core framework
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.10.1
python-dotenv==1.0.1

# HTTP client (LLM API calls)
httpx>=0.27.0

# Semantic retrieval — Hybrid RAG (dense + sparse)
sentence-transformers>=2.7.0
chromadb>=0.5.0
rank-bm25==0.2.2

# Schema graph — GraphRAG (FK relationship engine)
networkx==3.3

# Rate limiting
slowapi==0.1.9

# JWT authentication (production SSO)
PyJWT>=2.8.0
cryptography>=42.0.0

# Database connections
psycopg2>=2.9.9
python-dotenv==1.0.1
